import { Skill } from '../Skill';
import Player from '../player';
export default class Bumpercar extends Skill {
    used: boolean;
    constructor(owner: Player);
    execute(allPlayers: Player[]): void;
}
//# sourceMappingURL=bumpercar.d.ts.map